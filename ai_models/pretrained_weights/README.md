
# Pretrained Weights

This directory stores trained model checkpoints.

Run `python ai_models/train.py` to generate weights.

## Files
- `cnn_lstm_aqi.pth` — CNN-LSTM model state dict
- `training_metadata.json` — Training metadata
- `evaluation_metrics.json` — Test set evaluation results
- `evaluation_report.json` — Full evaluation report
